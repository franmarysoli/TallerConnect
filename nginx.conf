server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Necesario para React Router: redirige todas las rutas a index.html
        try_files $uri $uri/ /index.html;
    }
}
